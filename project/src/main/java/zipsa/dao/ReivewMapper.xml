<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="zipsa.dao.T_ReivewDAO"></mapper>
		
	<!--  리뷰작성하는 sql 아직미정 
	<insert id="write" parameterType="zipsa.entity.T_REVIEW">
		insert into T_REVIEW(
		
		)values(
		num_board.nextval,
		#{title},
		#{writer},
		#{filename},
		#{content},
		sysdate
		)
	</insert>
	
<!-- 전체리뷰보기 -->
	<select id="reviewselect" resultType="zipsa.entity.T_REVIEW"">
	SELECT * FROM T_REVIEW 
	</select>
	
<!--  예약번호 받아오기 -->
	<select id="reservNum" parameterType="zipsa.entity.T_MEMBER"
		resultType="zipsa.entity.T_RESERVATION">
		SELECT 
		WHERE M_id=#{id}
	</select>
	
	<!-- 리뷰 하나하나 보는 sql
	<select id="view" resultType="zipsa.entity.T_REVIEW"">
	SELECT * FROM WEB_BOARD WHERE num= #{num}</select>
	 -->
</mapper>